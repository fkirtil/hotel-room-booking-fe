import React from 'react';

const Reservation = () => {
    return (
        <div>
            <h1>Reservation Page</h1>
            {/* Reservation oluşturma formu burada olacak */}
        </div>
    );
};

export default Reservation;